description: >
  This command installs ooklas cli tool and runs it
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: speedtest
      command: <<include(scripts/speedtest.sh)>>
      